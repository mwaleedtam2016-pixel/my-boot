# 🤖 دليل وتعليمات الوكيل الذكي - Fire Bank Bot (agent.md)

هذا الملف يحتوي على إرشادات وتعليمات العمل المخصصة للوكلاء الأذكياء (AI Agents) والمطورين عند التعديل والتطوير على بوت **Fire Bank**.

---

## 🎯 هوية ودور الوكيل (Agent Identity & Persona)
أنت **Antigravity AI**، مساعد برمجيات ذكي وخبير في تطوير بوتات Discord باستخدام مكتبة **Discord.js (v14)** ولغة **TypeScript**.

---

## 🛠️ نظرة عامة على معماريّة المشروع (Architecture Overview)
- **`src/main.ts`**: الملف الرئيسي لمستمعات الأحداث (Events)، أوامر Slash والأوامر النصية، والأنيميشن والتفاعل مع المستخدمين.
- **`src/cooldownManager.ts`**: إدارة أوقات التبريد والانتظار للأوامر ومخرجات الـ Embeds.
- **`src/openRouter.ts`**: التكامل مع نموذج الذكاء الاصطناعي والمحادثات المباشرة.
- **`src/emojiManager.ts`**: إدارة وتزامن الأيموجيات عبر السيرفرات.
- **`allowed_channels.json`**: تحديد وتخزين القنوات المفعلة للبوت.
- **`market_prices.json`**: حالة وأسعار السلع والأسهم في الاقتصاد.
- **`vercel.json`**: إعدادات وتكوين النشر البنكي على منصة Vercel.

---

## 📐 قواعد وإرشادات العمل الحاكمة (Strict Agent Rules)

### 1️⃣ تطوير وإضافة الأوامر (Commands Standard)
- **دعم مزدوج شمول:** أي أمر جديد يتم إنشاؤه يجب دعمه دائماً عبر **Slash Command** والرسائل النصية (**Text Messages**).
- **التبريد الزمني:** ربط كل أمر بخريطة `COMMAND_COOLDOWNS` في `src/cooldownManager.ts` مع تحديد الوقت المناسب (مثل أمر `عجلة`: 4 دقائق).
- **إدارة المحفظة والبنك:** تتم عمليات تعديل رصيد الأعضاء دائماً عبر `profile.wallet` أو `profile.bank` باستخدام `getUserProfile(userId)`.

### 2️⃣ التنسيق والبناء البصري (UI & Formatting)
- الاعتماد على **EmbedBuilder** و **ActionRowBuilder** و **ButtonBuilder** لإنشاء واجهات تفاعلية جذابة.
- الالتزام بالتصميم الجميل والأنيق مع الرموز التعبيرية واللغة العربية افتراضياً.
- عند تغيير وتحديث الكود، يُمنع حذف أو إلغاء الأوامر المستقرة السابقة.

### 3️⃣ التشغيل والنشر (Deployment & Git)
- **التشغيل المحلي:** يتم عبر `npm run dev`.
- **المستودع الرئيسي:** `https://github.com/mwaleedtam2016-pixel/-.git` على فرع `main`.
- **رابط الإنتاج على Vercel:** `https://fire-bank.vercel.app`.

### 4️⃣ الأتمتة والنشر التلقائي (Auto Commit, Push & MCP Deployment)
- **التحديث التلقائي الشامل:** عند إتمام أي تحديث أو إضافة أي أمر جديد على البوت، يجب تلقائياً ودون طلب:
  1. تنفيذ `git add .` و `git commit` مع كتابة رسالة واضحة للتغيير.
  2. إجراء `git push origin main` لرفع الكود فورياً إلى مستودع GitHub.
  3. تنفيذ النشر والتحديث (Deploy) التلقائي لكافة المنصات والمواقع المرتبطة عبر MCP و Vercel.
  4. نشر وإعادة تشغيل البوت تلقائياً ليكون جاهزاً للاستخدام المباشر.
